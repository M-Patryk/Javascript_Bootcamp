//It retrives what the current styling value for the color, display, position etc. is
const li = document.querySelector('li')

//It will return the CSS Style Declaration (css values of HTML elements)
const liStyles = getComputedStyle(li)